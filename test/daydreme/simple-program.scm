(define add (lambda (x y) (+ x y))
(define square (lambda (x) (* x x))
(add (square 3) (square 4))

(add (if a b c) )

Frame:
- SchemeObject
- Environment
- Result

1. Evaluate List("square" ...)
1. Push "square" on to the stack
   1.1.